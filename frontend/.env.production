# Production build (Vercel → Ubuntu via Cloudflare Tunnel).
# REST uses same-origin /api/* rewritten in vercel.json.
# WebSocket cannot use Vercel rewrites, so hit the tunnel hostname directly.
VITE_WS_URL=wss://api-staging-eth-amm-sim.magro.dev/stream
