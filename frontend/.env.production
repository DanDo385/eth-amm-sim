# Production build (Vercel). REST uses same-origin /api/* → Cloudflare Tunnel
# rewrite in vercel.json. WebSocket cannot use Vercel rewrites, so hit the
# Ubuntu tunnel hostname directly.
VITE_WS_URL=wss://api-staging-eth-amm-sim.magro.dev/stream
